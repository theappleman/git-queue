From: Daniel Cordero <theappleman@gmail.com>

Remove free() to already freed data

transport->data is freed in disconnect_helper().

Signed-off-by: Daniel Cordero <theappleman@gmail.com>

diff --git c/transport-helper.c w/transport-helper.c
index 6182413..81fe8f4 100644
--- c/transport-helper.c
+++ w/transport-helper.c
@@ -186,7 +186,6 @@ static int release_helper(struct transport *transport)
 	free_refspec(data->refspec_nr, data->refspecs);
 	data->refspecs = NULL;
 	disconnect_helper(transport);
-	free(transport->data);
 	return 0;
 }
 
